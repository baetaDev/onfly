[mysqld]
# General logging for development
general_log = 1
general_log_file = /var/lib/mysql/general.log

# Performance settings
max_connections = 200
innodb_buffer_pool_size = 256M

# Character set
character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci
